import React, { useContext } from 'react';
import UserContext from '@/layouts/MenuContext';

function NewUser(props) {
  const { setChoiceList } = useContext(UserContext);
  return (
    <div />
  );
}

export default NewUser;